#include "main.h"

/**
 * mul - Multiples two integer
 * @a: The integer to be multipled.
 * @b: The integer to be multipled.
 *
 * Return: mul Always (Success)
 */
int mul(int a, int b)
{
int mul;
     mul = a * b;
     return(mul);
}
