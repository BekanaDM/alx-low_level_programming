#include "main.h"

/**
 * swap_int - swap the values of two integer
 * @a: variables checked
 * @b: variables 
 * @tmp: variables
 *
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	*temp = *a;
	*b = *a;
	*temp = *b;
}
